# Switch port

## Build

```
cmake --preset switch-devkitA64-release
cmake --build --preset switch-devkitA64-release
```

Needs `DEVKITPRO` set and NXVK installed to `portlibs/switch` (`NXVK_ROOT` override).
